# Explicitly set valid dev server host to avoid empty allowedHosts entries
HOST=localhost
# Optional: set a default port
PORT=3000
# Ensure webpack-dev-server allowedHosts is valid (v4 accepts 'all')
WDS_ALLOWED_HOSTS=all
