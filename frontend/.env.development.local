# Local-only overrides to ensure webpack-dev-server starts cleanly
# Highest precedence for CRA in development

# Force webpack-dev-server to accept any host to avoid empty allowedHosts issues
WDS_ALLOWED_HOSTS=all

# Last-resort safety for local development: disable host check entirely
# Do NOT use this in production builds
DANGEROUSLY_DISABLE_HOST_CHECK=true
